server:
  port: 8082
spring:
  application:
    name: turf-service
  datasource:
    url: jdbc:postgresql://localhost:5432/booknplay_turf
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  kafka:
    bootstrap-servers: localhost:9092
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
